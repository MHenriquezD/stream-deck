import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.mhenriquez.streamdeck',
  appName: 'stream-deck-app',
  webDir: 'dist',
}

export default config
