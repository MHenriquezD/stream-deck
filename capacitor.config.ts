import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.mhenriquez.spartanhub',
  appName: 'spartan-hub-app',
  webDir: 'dist',
}

export default config
