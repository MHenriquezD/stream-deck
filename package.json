{
  "name": "stream-deck-local",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n \"SERVER,WEB\" -c \"blue,green\" \"pnpm --filter server start:dev\" \"pnpm --filter web dev\"",
    "dev:server": "pnpm --filter server start:dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --parallel build:server build:web",
    "build:server": "pnpm --filter server build",
    "build:web": "pnpm --filter web build",
    "build:executables": "pnpm run build:server && pnpm run pkg:win && pnpm run pkg:mac && pnpm run pkg:linux",
    "pkg:win": "pkg apps/server/dist/main.js --targets node18-win-x64 --output dist/StreamDeck-Server-Windows.exe --compress GZip",
    "pkg:mac": "pkg apps/server/dist/main.js --targets node18-macos-x64 --output dist/StreamDeck-Server-macOS --compress GZip",
    "pkg:linux": "pkg apps/server/dist/main.js --targets node18-linux-x64 --output dist/StreamDeck-Server-Linux --compress GZip",
    "test": "jest -c jest.config.ts",
    "test:server": "pnpm --filter server test",
    "test:server:watch": "pnpm --filter server test:watch",
    "test:server:e2e": "pnpm --filter server test:e2e",
    "lint:server": "pnpm --filter server lint",
    "format:server": "pnpm --filter server format"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.0",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "pkg": "^5.8.1",
    "typescript": "^5.9.3"
  }
}
